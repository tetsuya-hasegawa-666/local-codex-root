# agents.md

## --testlogs の役割

- この階層は、project ごとの test 要約、verification summary、最小限の manifest を保持する。

## 運用規則

- raw JSON log、binary、HTML report、screen capture、export file は `--exsams/` へ置く。
- この階層には summary と必要最小限の pointer のみを残す。
