# inactive note

この file は旧 standalone `#12 Chunk DA3 Prepose Build` 説明の残骸であり、active runbook では使わない。
現在は `#11-5` が batch execution items を準備し、`#7-4` と `#7-5` が chunk-local `DA3 NGL` pose / GS build として `#7 Full Prepose Build` に吸収済みである。
