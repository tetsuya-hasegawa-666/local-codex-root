# classification criteria

## 既存 merge

- 既存 skill の目的、入力、出力、非責務を壊さずに拡張できる
- trigger 条件が既存 skill の領域に自然に入る
- references と scripts の追加で閉じる

## 新規 skill

- 既存 skill へ入れると責務がぶれる
- trigger 条件、入出力、close 条件が独立している
- 専用 ledger、専用 scripts、専用 references が必要
